git clone <git url>

   18  ll
   19  git status 			## check the status of the git changes in  local
   20  git add . 			## add new files to git repos if newly added 
   22  git config --global user.email "sagargv1997@gmail.com"
   23   git config --global user.name  "sagar"
   24  vim ~/.gitconfig
   21  git commit -m "added readme.txt" . ## commit the changes to  the local repo 
   26  git push 			## push the changes to git server 

